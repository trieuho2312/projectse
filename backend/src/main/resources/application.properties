spring.application.name=project_20251_backend

# Database
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-lingering-snow-a1nt9ps4-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.driver-class-name=${DATABASE_DRIVER:org.postgresql.Driver}
spring.datasource.username=${DATABASE_USERNAME:neondb_owner}
spring.datasource.password=${DATABASE_PASSWORD:npg_PHcRu0rs6DhS}

# IMPORTANT: Đổi từ create sang update để không mất dữ liệu
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway Configuration
spring.flyway.baseline-on-migrate=${FLYWAY_BASELINE_ON_MIGRATE:true}
spring.flyway.baseline-version=${FLYWAY_BASELINE_VERSION:0}
spring.flyway.enabled=${FLYWAY_ENABLED:true}

# JWT
jwt.signerKey=${JWT_SIGNER_KEY:affd3b4c455f135fcb756589d05d8dab799605a3a2aa39d53957d741bd0d857e}
jwt.valid-duration=${JWT_VALID_DURATION:15}
jwt.refreshable-duration=${JWT_REFRESHABLE_DURATION:7}

# Email
spring.mail.host=${MAIL_HOST:smtp.office365.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:Huy.NQ235947@sis.hust.edu.vn}
spring.mail.password=${MAIL_PASSWORD:KFhnzv26}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:true}

# PayOS Configuration
payos.client-id=${PAYOS_CLIENT_ID:YOUR_PAYOS_CLIENT_ID}
payos.api-key=${PAYOS_API_KEY:YOUR_PAYOS_API_KEY}
payos.checksum-key=${PAYOS_CHECKSUM_KEY:YOUR_PAYOS_CHECKSUM_KEY}
payos.base-url=${PAYOS_BASE_URL:https://api-merchant.payos.vn}
